<%# locals: (import:) %>

<%= styled_form_with model: @import, url: import_configuration_path(@import), scope: :import, method: :patch, class: "space-y-4" do |form| %>
  <div class="space-y-4">
    <div class="flex items-center gap-4">
      <%= form.select :date_col_label, import.csv_headers, { include_blank: t(".date_blank"), label: t(".date_label") }, required: true %>
      <%= form.select :date_format, Family::DATE_FORMATS, { label: t(".date_format_label")}, label: true, required: true %>
    </div>

    <div class="flex items-center gap-4">
      <%= form.select :qty_col_label, import.csv_headers, { include_blank: t(".quantity_blank"), label: t(".quantity_label") }, required: true %>
      <%= form.select :signage_convention, [[t(".buys_positive"), "inflows_positive"], [t(".buys_negative"), "inflows_negative"]], label: true, required: true %>
    </div>

    <div class="flex items-center gap-4">
      <%= form.select :currency_col_label, import.csv_headers, { include_blank: t(".currency_blank"), label: t(".currency_label") } %>
      <%= form.select :number_format, Import::NUMBER_FORMATS.keys, { label: t(".format_label"), prompt: t(".format_prompt") }, required: true %>
    </div>

    <%= form.select :ticker_col_label, import.csv_headers, { include_blank: t(".ticker_blank"), label: t(".ticker_label") }, required: true %>
    <%= form.select :exchange_operating_mic_col_label, import.csv_headers, { include_blank: t(".exchange_blank"), label: t(".exchange_label") } %>
    <%= form.select :price_col_label, import.csv_headers, { include_blank: t(".price_blank"), label: t(".price_label") }, required: true %>

    <% unless import.account.present? %>
      <%= form.select :account_col_label, import.csv_headers, { include_blank: t(".account_blank"), label: t(".account_label") } %>
    <% end %>

    <%= form.select :name_col_label, import.csv_headers, { include_blank: t(".name_blank"), label: t(".name_label") } %>

    <% unless Security.provider %>
      <div class="alert alert-warning">
        <p>
          <strong><%= t(".security_provider_note_label") %></strong> <%= t(".security_provider_note") %>
        </p>
      </div>
    <% end %>
  </div>

  <%= form.submit t(".submit"), disabled: import.complete? %>
<% end %>
