<%# locals: (import:) %>

<%= styled_form_with model: @import, url: import_configuration_path(@import), scope: :import, method: :patch, class: "space-y-4" do |form| %>
  <%= form.select :entity_type_col_label, import.csv_headers, { include_blank: t(".entity_type_blank"), label: t(".entity_type_label") } %>
  <%= form.select :name_col_label, import.csv_headers, { include_blank: t(".name_blank"), label: t(".name_label") }, required: true %>
  <%= form.select :amount_col_label, import.csv_headers, { include_blank: t(".balance_blank"), label: t(".balance_label") }, required: true %>
  <%= form.select :currency_col_label, import.csv_headers, { include_blank: t(".currency_blank"), label: t(".currency_label") } %>

  <%= form.submit t(".submit"), disabled: import.complete? %>
<% end %>
