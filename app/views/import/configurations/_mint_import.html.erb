<%# locals: (import:) %>

<div class="flex items-center justify-between border border-secondary rounded-lg bg-green-500/5 p-5 gap-4 mb-4">
  <span class="text-green-500">
    <%= icon("check-circle", color: "current") %>
  </span>
  <p class="text-sm text-primary italic"><%= t(".preconfigured_notice") %></p>
</div>

<%= styled_form_with model: @import, url: import_configuration_path(@import), scope: :import, method: :patch, class: "space-y-4" do |form| %>
  <div class="flex items-center gap-4">
    <%= form.select :date_col_label, import.csv_headers, { include_blank: t(".date_blank"), label: t(".date_label") }, required: true, disabled: import.complete? %>
    <%= form.select :date_format, Family::DATE_FORMATS, { label: t(".date_format_label")}, label: true, required: true, disabled: import.complete? %>
  </div>

  <div class="flex items-center gap-4">
    <%= form.select :amount_col_label, import.csv_headers, { include_blank: t(".amount_blank"), label: t(".amount_label") }, required: true, disabled: import.complete? %>
    <%= form.select :signage_convention, [[t(".incomes_negative"), "inflows_negative"], [t(".incomes_positive"), "inflows_positive"]], { label: true }, disabled: import.complete? %>
  </div>

  <div class="flex items-center gap-4">
    <%= form.select :currency_col_label, import.csv_headers, { include_blank: t(".currency_blank"), label: t(".currency_label") }, disabled: import.complete? %>
    <%= form.select :number_format, Import::NUMBER_FORMATS.keys, { label: t(".format_label"), prompt: t(".format_prompt") }, required: true %>
  </div>

  <% unless import.account.present? %>
    <%= form.select :account_col_label, import.csv_headers, { include_blank: t(".account_blank"), label: t(".account_label") }, disabled: import.complete? %>
  <% end %>

  <%= form.select :name_col_label, import.csv_headers, { include_blank: t(".name_optional_blank"), label: t(".name_optional_label") }, disabled: import.complete? %>
  <%= form.select :category_col_label, import.csv_headers, { include_blank: t(".category_blank"), label: t(".category_label") }, disabled: import.complete? %>
  <%= form.select :tags_col_label, import.csv_headers, { include_blank: t(".tags_blank"), label: t(".tags_label") }, disabled: import.complete? %>

  <%= form.submit t(".submit"), disabled: import.complete? %>
<% end %>
